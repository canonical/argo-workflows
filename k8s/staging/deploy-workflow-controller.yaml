apiVersion: apps/v1
kind: Deployment
metadata:
  name: workflow-controller
spec:
  template:
    spec:
      containers:
      - args:
        - --configmap
        - workflow-controller-configmap
        - --executor-image
        - stg-commercial-systems.docker-registry.canonical.com/argo-ubuntu-exec
        - --namespaced
